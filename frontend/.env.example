# ── Frontend env (คัดลอกเป็น .env / .env.local แล้วแก้ค่าให้ตรงระบบ) ──

# โหมดเดโม (ใช้ MSW mock แทน backend จริง) — true = เดโม, ว่าง/false = ยิง API จริง
VITE_DEMO_MODE=false

# URL ระบบ Traceability ภายนอก (KNEX_GW) — ห้าม hardcode ใน source (FE-15)
VITE_JIGAPI_URL=https://jig-api.syntechnology.com
