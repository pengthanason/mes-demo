node_modules/
__pycache__/
*.pyc
# env files — never commit (contain DB password / JWT secret)
.env
.env.*
!.env.example
dist/
build/
.next/
*.log
tmp/
*.db

# local-only overrides for docker compose (personal dev settings — never shared/committed)
docker-compose.override.yml
